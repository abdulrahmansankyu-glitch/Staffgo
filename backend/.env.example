DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
JWT_ACCESS_SECRET="change-me"
JWT_REFRESH_SECRET="change-me"
PORT=4000
DEFAULT_VAT_RATE=15
DEFAULT_CURRENCY=SAR
# Set in production so cookies/CORS are locked to the deployed frontend origin, e.g. https://staffgo.netlify.app
FRONTEND_URL=

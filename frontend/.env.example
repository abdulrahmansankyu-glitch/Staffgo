# Set in Netlify's build environment to your deployed Render backend URL, e.g. https://staffgo-backend.onrender.com
# Leave unset for local dev (Vite proxies /api to localhost:4000) or single-origin deployment.
VITE_API_URL=
